export interface QRCodePluginOptions {
	/**
	 * Port number of the dev server
	 * @default 3000
	 */
	port?: number;

	/**
	 * Base path of the application (e.g., '/app')
	 * @default ''
	 */
	path?: string;
}
